{
  "name": "policy-prism",
  "private": true,
  "version": "1.0.0",
  "description": "Policy Prism - hospital policy coverage analysis against regulatory requirement libraries",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "install:all": "npm install",
    "build:shared": "npm run build --workspace shared",
    "dev": "concurrently -n server,client -c blue,green \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "build": "npm run build --workspace shared && npm run build --workspace client && npm run build --workspace server",
    "start": "npm run start --workspace server",
    "db:generate": "npm run db:generate --workspace server",
    "db:migrate": "npm run db:migrate --workspace server",
    "db:push": "npm run db:push --workspace server",
    "db:seed": "npm run db:seed --workspace server",
    "db:reset": "npm run db:reset --workspace server",
    "setup": "npm install && npm run build:shared && npm run db:migrate && npm run db:seed",
    "typecheck": "npm run build --workspace shared && npm run typecheck --workspace client && npm run typecheck --workspace server",
    "verify": "npm run typecheck && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "overrides": {
    "@types/express-serve-static-core": "4.19.6"
  }
}
